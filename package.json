{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Backend && cd src && node src/server.js",
    "client": "cd Frontend && cd src && npm run dev"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
